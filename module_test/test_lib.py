import unittest
import os

import app.lib.lib as lib


class TestLib(unittest.TestCase):

    def test_recursive_create_folder(self) -> None:
        return

        # cwd = os.getcwd()
        # lib.recursively_create_folders(os.sep.join(['a', 'b', 'c']))
        # os.chdir(cwd)

