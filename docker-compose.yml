version: "3.9"

services:
  voice_assistant:
    build: .
    stdin_open: true
    tty: true
    user: 'root'
    volumes:
      - ./:/app
      - ./asound.conf:/etc/asound.conf
    devices:
      - /dev/snd:/dev/snd
    environment:
      - LANG=C.UTF-8
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - OPENWEATHER_API_KEY=${OPENWEATHER_API_KEY}
    networks:
      - testing

networks:
  testing:
    external: true
